// userだけど、customerやbuyerではいけないのか
let user = authenticate(credentials)


// deductPercentは、何を実行するかは表しているが、なぜ実行するかを表してない
// amoutというパラメーター名が誤解を生みやすい
public void deductPercent(double amount)

public void applyDiscount(Percentager discount)
